info:
  name: findOne
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/professions/{{professionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Não exige autenticação. Inclui violationCategories no retorno.
