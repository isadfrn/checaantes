info:
  name: findAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/professions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Não exige autenticação.
