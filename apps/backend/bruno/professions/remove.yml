info:
  name: remove
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/professions/{{professionId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Exige role=admin.
